# Base URL the Axios client points at.
# In dev, leave as-is: Vite proxies /api -> http://localhost:5000 (see vite.config.js).
VITE_API_URL=/api/v1

# Backend origin the Vite dev-server proxy forwards to (dev only).
VITE_API_PROXY=http://localhost:5000
